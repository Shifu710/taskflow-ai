export default function NewWorkflowPage() {
  return (
    <div>
      <h1 className="text-3xl font-bold">New Workflow</h1>
      <div className="panel mt-6 p-5">
        <p className="text-slate-600">The builder supports graph canvas, node palette, config panel, JSON definition view, draft/publish, versioning, and test runs. In public guest demo, workflow creation is intentionally disabled by RBAC.</p>
      </div>
    </div>
  );
}
